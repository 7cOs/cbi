import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CompassTabsComponent } from './compass-tabs.component';

describe('CompassTabsComponent', () => {

  let fixture: ComponentFixture<CompassTabsComponent>;
  let componentInstance: CompassTabsComponent;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [ CompassTabsComponent ]
    });

    fixture = TestBed.createComponent(CompassTabsComponent);
    componentInstance = fixture.componentInstance;
  });
});
