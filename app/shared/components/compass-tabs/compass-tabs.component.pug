.compass-tabs-container
  ul(class="nav-tabs")
    li.compass-tab(
      *ngFor="let tab of tabs",
      [ngClass]="{'active-tab': tab.active}",
      (click)="selectTab(tab)"
    )
      p {{ tab.title }}

ng-content
