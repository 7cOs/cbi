section.filter-component-section

  //- Metric Select
  div
    compass-select(
      [model]="filterState.metricType",
      [options]="metricOptions",
      [title]="'metric'",
      (onOptionSelected)="filterSelected(myPerformanceFilterActionType.Metric, $event)"
    )

  //- Time Period Select (Depletions)
  div(*ngIf="filterState.metricType === metricTypeValue.Depletions")
    compass-select(
      [model]="filterState.dateRangeCode",
      [options]="depletionTimePeriodOptions",
      [title]="'time period'",
      (onOptionSelected)="filterSelected(myPerformanceFilterActionType.TimePeriod, $event)"
    )

  //- Time Period Select (Distribution)
  div(*ngIf="filterState.metricType !== metricTypeValue.Depletions")
    compass-select(
      [model]="filterState.dateRangeCode",
      [options]="distributionTimePeriodOptions",
      [title]="'time period'",
      (onOptionSelected)="filterSelected(myPerformanceFilterActionType.TimePeriod, $event)"
    )

  //- Premise Type Radio (Depletions)
  div(*ngIf="filterState.metricType === metricTypeValue.Depletions")
    compass-radio(
      [model]="filterState.premiseType",
      [options]="depletionsPremiseOptions",
      [title]="'premise type'",
      [stacked]="false",
      (onRadioClicked)="filterSelected(myPerformanceFilterActionType.PremiseType, $event)",
    )

  //- Premise Type Radio (Distribution)
  div(*ngIf="filterState.metricType !== metricTypeValue.Depletions")
    compass-radio(
      [model]="filterState.premiseType",
      [options]="distributionPremiseOptions",
      [title]="'premise type'",
      [stacked]="filterState.metricType === metricTypeValue.Distribution",
      (onRadioClicked)="filterSelected(myPerformanceFilterActionType.PremiseType, $event)"
    )

  //- Distribution Type Radio
  div(*ngIf="filterState.metricType === metricTypeValue.Distribution")
    compass-radio(
      [model]="filterState.distributionType",
      [options]="distributionOptions",
      [title]="'distribution type'",
      [stacked]="true",
      (onRadioClicked)="filterSelected(myPerformanceFilterActionType.DistributionType, $event)"
    )
