section.filter-component-section

  //- Metric Select
  div
    compass-select(
      [model]="filterState.metric",
      [options]="metricOptions",
      [displayKey]="'metricName'",
      [valueKey]="'metricValue'",
      [title]="'metric'",
      (onOptionSelected)="filterSelected('metric', $event)"
    )

  //- Time Period Select (Depletions)
  div(*ngIf="filterState.metric === metricValueEnum.DEPLETIONS")
    compass-select(
      [model]="filterState.timePeriod",
      [options]="depletionTimePeriodOptions",
      [displayKey]="'displayCode'",
      [valueKey]="'codeValue'",
      [subDisplayKey]="'range'",
      [title]="'time period'",
      (onOptionSelected)="filterSelected('timePeriod', $event)"
    )

  //- Time Period Select (Distribution)
  div(*ngIf="filterState.metric !== metricValueEnum.DEPLETIONS")
    compass-select(
      [model]="filterState.timePeriod",
      [options]="distributionTimePeriodOptions",
      [displayKey]="'displayCode'",
      [valueKey]="'codeValue'",
      [subDisplayKey]="'range'",
      [title]="'time period'",
      (onOptionSelected)="filterSelected('timePeriod', $event)"
    )

  //- Premise Type Radio (Depletions)
  div(*ngIf="filterState.metric === metricValueEnum.DEPLETIONS")
    compass-radio(
      [model]="filterState.premiseType",
      [options]="depletionsPremiseOptions",
      [displayKey]="'premiseType'",
      [valueKey]="'premiseTypeValue'",
      [title]="'premise type'",
      [direction]="'inline'",
      (onRadioClicked)="filterSelected('premiseType', $event)",
    )

  //- Premise Type Radio (Distribution)
  div(*ngIf="filterState.metric !== metricValueEnum.DEPLETIONS")
    compass-radio(
      [model]="filterState.premiseType",
      [options]="distributionPremiseOptions",
      [displayKey]="'premiseType'",
      [valueKey]="'premiseTypeValue'",
      [title]="'premise type'",
      [direction]="filterState.metric === metricValueEnum.DISTRIBUTION ? 'stacked' : 'inline'",
      (onRadioClicked)="filterSelected('premiseType', $event)"
    )

  //- Distribution Type Radio
  div(*ngIf="filterState.metric === metricValueEnum.DISTRIBUTION")
    compass-radio(
      [model]="filterState.distributionType",
      [options]="distributionOptions",
      [displayKey]="'distributionType'",
      [valueKey]="'distributionTypeValue'",
      [title]="'distribution type'",
      [direction]="'stacked'",
      (onRadioClicked)="filterSelected('distributionType', $event)"
    )
