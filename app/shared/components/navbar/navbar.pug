.archived-header.success(ng-class="{'visible': nb.archived}") Target List<span ng-if="nb.multipleTargetListsSelected">s</span> Archived!
.archived-header.success(ng-class="{'visible': nb.deleted}") Target List<span ng-if="nb.multipleTargetListsSelected">s</span> Deleted!
.archived-header.success(ng-class="{'visible': nb.added}") Opportunity Added!
.archived-header.success(ng-class="{'visible': nb.copied}") <span>{{nb.multipleTargetListsSelected ? 'Opportunities' : 'Opportunity'}}</span> added to target list!
.archived-header(ng-class="{'visible': nb.deleteError}") Sorry, you cannot delete a Target List that has Collaborators. Please remove all Collaborators from that list and try again.

div.outer-nav-wrapper
  div.inner-nav-wrapper(container)
    div(row)
      img(src="assets/img/logos/Compass_logo.svg", ui-sref="landing", alt="Home" analytics-on="click" analytics-event="Image Click" analytics-category="Navigation" analytics-label="Compass Logo").nav-logo
      .nav-logo-background
      div.nav-link-wrapper
        ul(column='8').main-nav
          li.top-level-menu(ui-sref-active="active")
            div.inner
              span #[a(ui-sref="landing" analytics-on="click" analytics-event="Link Click" analytics-category="Navigation" analytics-label="Home") Home]
          li.top-level-menu.parent(ui-sref-active="active")
            div.inner
              span #[a My Performance]
            ul.dropdown
              li.sub-menu(ui-sref="scorecards" analytics-on="click" analytics-event="Link Click" analytics-category="Navigation" analytics-label="My Scorecards") #[a My Scorecards]
              li.sub-menu(ui-sref="accounts" analytics-on="click" analytics-event="Link Click" analytics-category="Navigation" analytics-label="Account Dashboard") #[a Account Dashboard]
          li.top-level-menu(ui-sref-active="active")
            div.inner
              span #[a(ui-sref="opportunities" analytics-on="click" analytics-event="Link Click" analytics-category="Navigation" analytics-label="Opportunities") Opportunities]
          li.top-level-menu(ui-sref-active="active")
            div.inner
              span #[a(ui-sref="target-lists" analytics-on="click" analytics-event="Link Click" analytics-category="Navigation" analytics-label="Target Lists") Target Lists]
          // li.top-level-menu(ui-sref-active="active")
            div.inner
              span #[a(ui-sref="help") Help]
        div.nav-icon-container
          .notification-count-badge(ng-if="nb.notificationHelper.showBadge")
            span {{nb.notificationHelper.unseenNotifications}}
          a.nav-icon.add-opportunity(ng-click="nb.modalAddOpportunityForm(); nb.getTargetLists()" analytics-on="click" analytics-event="Add Opportunity" analytics-category="Opportunities" analytics-label="Navbar Icon Click")
            inline-search(ng-if="nb.accountSelectorSelected === 'Store'" type="store" placeholder="Search for a store" chosen-result="nb.searchResult" nav="true")
          md-menu(md-position-mode="target-right target" md-offset="1 38")
            a.nav-icon.notifications(href="#" md-menu-origin ng-click="$mdOpenMenu($event); nb.markSeen(nb.notifications)", aria-label="Notifications" analytics-on="click" analytics-event="View Notification" analytics-category="Notifications" analytics-label="Navbar Icon Click")
            md-menu-content.nav-flyout
                .content.nopad
                  div.notification-card.clearfix(md-prevent-menu-close ng-repeat="notification in nb.notifications | orderBy: '-dateCreated'" ng-class="{'read': notification.status === 'READ'}" ng-click="nb.markRead(notification)" class="{{notification.action}}")
                    .details
                      div(ng-if="notification.objectType.toUpperCase() === 'TARGET_LIST'")
                        p You've been added as a collaborator
                        .source {{notification.shortenedObject.name}}
                      div(ng-if="notification.objectType.toUpperCase() === 'OPPORTUNITY'")
                        p An opportunity has been shared with you
                        .source {{notification.shortenedObject.store.store_name | titlecase}}
                          span(ng-if="notification.shortenedObject.store.store_number.toUpperCase() !== 'UNKNOWN'")  \#{{notification.shortenedObject.store.store_number}}
                        p.sku {{notification.shortenedObject.product.name | titlecase}}
                          span  {{notification.shortenedObject.type | formatOpportunitiesType}}
                      div(ng-if="notification.objectType.toUpperCase() === 'STORE'" ng-click="nb.goToNote(notification)")
                        p A new note was added to your account
                        .source {{notification.shortenedObject.store_name | titlecase}}
                          span(ng-if="notification.shortenedObject.store_number && notification.shortenedObject.store_number.toUpperCase() !== 'UNKNOWN'") &nbsp\#{{notification.shortenedObject.store_number}}
                      div(ng-if="notification.objectType.toUpperCase() === 'DISTRIBUTOR'" ng-click="nb.goToNote(notification)")
                        p A new note was added to your account
                        .source {{notification.shortenedObject.name | titlecase}}
                    .info
                      label {{notification.dateCreated | timeAgo: 'relative'}}
                      label.bold by {{notification.creator.firstName}} {{notification.creator.lastName}}
                  div.notification-card.clearfix(ng-if="nb.notificationHelper.unreadNotifications < 1")
                    .details
                      p {{nb.noNotifications}}
          md-menu(md-position-mode="target-right target" md-offset="2 54")
            a.nav-icon.settings(href="#" md-menu-origin ng-click="$mdOpenMenu($event)", aria-label="Settings" analytics-on="click" analytics-event="View Settings" analytics-category="Settings" analytics-label="Navbar Icon Click")
            md-menu-content
              .nav-flyout
                .content
                  p You've logged in as
                  p <strong>{{nb.userService.model.currentUser.firstName | titlecase}} {{nb.userService.model.currentUser.lastName | titlecase}}</strong>
                  a(href="/auth/logout" target="_self" analytics-on="click" analytics-event="Button Click" analytics-category="Navigation" analytics-label="Logout")
                    button.btn-action Logout
          div.version {{nb.versionService.model.version.hash}} | {{nb.versionService.model.version.version}}
            a.feedback(href="#" ng-click="nb.sendFeedback()" analytics-on="click" analytics-event="Link Click" analytics-category="Navigation" analytics-label="Send Feedback") Send Feedback
      ul.nav-sub-menu(ng-if="$root.pageTitle === 'Account Dashboard' || $root.pageTitle === 'Scorecards'")
        li.accent #[a(ui-sref="scorecards" ui-sref-active="active" analytics-on="click" analytics-event="Link Click" analytics-category="Navigation" analytics-label="My Scorecards") My Scorecards]
        li.accent #[a(ui-sref="accounts" ui-sref-active="active" analytics-on="click" analytics-event="Link Click" analytics-category="Navigation" analytics-label="Account Dashboard") Account Dashboard]
