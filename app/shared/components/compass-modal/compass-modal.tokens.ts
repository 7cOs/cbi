import { InjectionToken } from '@angular/core';

import { CompassModalInputs } from '../../../models/compass-modal-inputs.model';

export const COMPASS_MODAL_INPUTS = new InjectionToken<CompassModalInputs>('COMPASS_MODAL_INPUTS');
