span([ngClass]="getBackButtonClass()", (click)="backButtonClicked.emit()")
.breadcrumb-container
  span.breadcrumb-entity(
  *ngFor="let entity of breadcrumbTrail",
  (click)="breadcrumbEntityClicked.emit({trail: breadcrumbTrail, entity: entity})") {{ entity }}
