section.compass-tooltip
    .div
      .tooltip
        label {{ label }}
          img(src="../../../assets/img/icons/help-tooltip.svg")
          span.tooltiptext(class='{{ position }}')
            h3(ng-if="this.title") {{ this.title }}
            p {{ this.description }}
