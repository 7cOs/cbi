describe('Unit: productsService', function() {
  require('angular-mocks/ngMock');

  /* var httpBackend, service, apiService;

  beforeEach(function() {
    angular.mock.module('andromeda.common.services');

    angular.mock.inject(function GetDependencies(apiHelperService) {
      apiService = apiHelperService;
    });

  });

  beforeEach(inject(function(productsService, $httpBackend) {
    service = productsService;
    httpBackend = $httpBackend;
  }));

  it('should have services defined', function() {
    // console.log(service);
    // expect(q).not.toBeUndefined();
    // expect(http).not.toBeUndefined();
    // expect(helperService).not.toBeUndefined();
  });*/

});
